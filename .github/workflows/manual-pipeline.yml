name: Manual Full Pipeline Trigger

on:
  workflow_dispatch:

jobs:
  trigger:
    name: Start Manual Chain
    runs-on: ubuntu-latest
    steps:
      - name: Manual Trigger
        run: |
          echo "Activating manual pipeline chain..."
          echo "Event triggered by: ${{ github.actor }}"
